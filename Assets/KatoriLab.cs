using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class KatoriLab : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        Data.Instance.referer="KatoriLab";
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
