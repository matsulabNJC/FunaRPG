using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Map2Scene : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        Data.Instance.referer="Map2Scene";
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
